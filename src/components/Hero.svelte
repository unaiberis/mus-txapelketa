<script>
  export let title = 'Your event, beautifully bracketed';
  export let lead = 'Create, share and run tournaments with a tiny footprint — accessible, responsive, and delightful.';
  // CTA items: [{ label, href, variant }]
  export let ctas = [{ label: 'Get started', href: '/koadroa', variant: 'primary' }];

</script>

<!-- Hero markup removed per task: component intentionally renders nothing -->
